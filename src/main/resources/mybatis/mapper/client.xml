<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.callbus.kyh.mapper.ClientMapper">


    <select id="findById" resultType="ClientDTO">
        SELECT A.id
            , A.name
            , A.phone_number
            , A.push_agree
            , B.type
            , A.register_date
            , A.update_date
        FROM CLIENT A
        INNER JOIN CLIENT_TYPE B ON A.client_type_id = B.id
        WHERE A.id = #{id}
    </select>

</mapper>